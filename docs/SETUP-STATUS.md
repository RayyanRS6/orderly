# Account setup status

## Update — September 17, 2026

The audit fixes and UI from `Claude-UI-Rayyan` are deployed on the existing Cloudflare/Supabase stack. Both launch migrations are applied; 17 application tables have RLS enabled. Laziza remains paused with no integrations and no live orders. The scheduler is configured. No paid plans or domain were purchased. See the [implementation report](ORDERLY-IMPLEMENTATION-2026-09-17.md) for verification, current capabilities and remaining provider/onboarding work.

## Historical setup record — September 13, 2026

- Cloudflare + Supabase architecture is deployed. Backend bundle (1.37 MB) runs in the hosted Deno runtime; 132 tests and TypeScript checks pass. Scheduler migration is applied and Vault worker credentials are configured. No paid upgrades purchased.
- Cloudflare account `05bf42b85732aa69af0da89eaa4c871f` under `waytogalaxy999@gmail.com` authorized Wrangler through device sign-in. Frontend: `https://orderly.waytogalaxy999.workers.dev`, version `d99ab118-2bf5-4a0a-a554-5edbd161835f`. Backend: `https://zygsuxgqkeedgcbjhfzx.supabase.co/functions/v1/orderly`. Auth site/redirect URL and exact-origin CORS point to this frontend. Live login page visibly loads. Supabase deployment access was supplied through a loopback-only token form and is stored in ignored `.local/deploy.env`.
- Hosted checks passed: real password login, company onboarding, menu CRUD, order confirmation and duplicate protection, cross-tenant API/RLS isolation, restricted secret/scheduler access, immediate pg_net dispatch, actual minute recovery and expired-lease recovery. Disposable users and companies were removed by exact IDs. Results are in ignored `.local/hosted-validation-result.json`.

- Supabase project `Orderly`: `zygsuxgqkeedgcbjhfzx`, MeeruRayyan Free organization. Automatic Asia-Pacific placement selected Sydney (`ap-southeast-2`).
- Both initial SQL migrations applied successfully together in a transaction through the SQL editor. Live verification: 13 public tables, all 13 with RLS enabled. SQL editor execution does not populate Supabase CLI migration history; do not blindly push these migrations again.
- Existing anon and service-role keys saved in ignored local `.env`. Verified anon cannot read companies (401); service role reads the empty companies table (200).
- Gemini `Orderly server` key is deployed as a server secret. Google AI Studio confirmed Free tier. The old `gemini-2.5-flash-lite` returns 404 for this account; the default is now `gemini-3.5-flash-lite`, which passed actual generation and hosted natural-language ordering. The model receives the canonical action schema explicitly as well as provider structured-output settings; all actions remain strictly validated. This key belongs to Google project `gen-lang-client-0579034647` and shares quota with Proofix. Application usage estimates use paid rates and are not provider invoices.
- Local app remains in demo mode, preserving existing demo data. Founder `waytogalaxy999@gmail.com` was created and confirmed by the user; platform-admin membership is enabled for `03df5c0d-9a6c-4d15-a899-bdf0ba180489`.
- Private GitHub repository `https://github.com/RayyanRS6/orderly` created and code pushed using the existing RayyanRS6 Git Credential Manager login. Credential scan found none of the saved keys in tracked source.
- Legacy Vercel project `orderly` was created in Rayyan Meeru Team with eight environment variables from ignored `.local/vercel.env`; it is no longer the deployment target. Vercel files and queue dependency are removed from the app. Keep the existing encryption key when moving server secrets to Supabase. The old cloud project has not been deleted or disconnected.
- Google Sheets API is enabled. Service account `orderly-sheets@gen-lang-client-0579034647.iam.gserviceaccount.com` was created without project roles; user approved its JSON key creation and encrypted app storage. Key is saved in ignored `.local/google-sheets.json`; downloaded key filenames are also ignored. It has Editor access only to the dedicated test spreadsheet shared with it.
- Persistent pilot company: **Orderly Test Restaurant**, ID `b390a43e-c55a-4d3c-9296-7d5f8b931080`. Connected spreadsheet: `https://docs.google.com/spreadsheets/d/1A1zMmdeMfCW8TQXfeKEBm5pH0JAszWXGtfNl2X9uAKw/edit`, with Menu and Orders tabs, four sample items, and Gemini 3.5 Flash-Lite. Hosted test passed: natural-language request → review → explicit confirmation → Rs. 900 order → Sheet row → accepted status update on the same row. Synthetic test order retained; temporary login removed. Result: ignored `.local/pilot-test-result.json`.
- Meta developer contact verification now works. WhatsApp app creation requires a business portfolio. Business Suite initially required a Facebook Page; user approved Page creation and Meta Pages policies. Page **Orderly Business Chatbots** (ID `1355389687654741`) was created; Meta rejected the shorter name as too broad. User approved the portfolio's Commercial Terms and Create was submitted with matching name, contact Meerub Salman and `waytogalaxy999@gmail.com`. Meta rejected creation with **Your advertising access is restricted**, explicitly stating this account cannot create business portfolios. The portfolio was not created. Business Support Home also confirms the Facebook account is restricted, and lists its personal ad account restriction as October 10, 2021. No Request review action is shown on the account detail page. Meta account review is required before this setup can continue. The user authorized continuing the support form, including its Beta Product Testing and AI terms. Submitted a factual description and selected the affected Facebook profile; Meta returned a help article only, without a case number or human support/review action. Support page: https://www.facebook.com/business-support-home/100064293579485/?source=link. No WhatsApp API number or Meta credentials are connected yet.

Account assignments: Supabase/Vercel `waytogalaxy999@gmail.com`; GitHub `RayyanRS6` / `meerurayyan@gmail.com`; Google services `meerubmellow@gmail.com`.

Never commit `.env` or `.local`. The temporary loopback setup helper is in ignored `.local/setup-server.cjs`; stop it after collecting credentials.
